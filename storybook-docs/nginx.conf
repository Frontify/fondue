# Main storybook-docs site
server {
    listen 80 default_server;
    server_name ffy-fondue.test *.ffy-fondue.test;

    root /usr/share/nginx/dist/docs;
    index index.html;

    add_header Access-Control-Allow-Origin $http_origin always;
    add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
    add_header Access-Control-Allow-Headers * always;
    add_header Access-Control-Allow-Credentials true always;

    location / {
        if ($request_method = OPTIONS) {
            return 204;
        }
        try_files $uri $uri/ /index.html;
    }
}

# Components
server {
    listen 80;
    server_name components.ffy-fondue.test *.components.ffy-fondue.test;

    root /usr/share/nginx/dist/components;
    index index.html;

    add_header Access-Control-Allow-Origin $http_origin always;
    add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
    add_header Access-Control-Allow-Headers * always;
    add_header Access-Control-Allow-Credentials true always;

    location / {
        if ($request_method = OPTIONS) {
            return 204;
        }
        try_files $uri $uri/ /index.html;
    }
}

# Charts
server {
    listen 80;
    server_name charts.ffy-fondue.test *.charts.ffy-fondue.test;

    root /usr/share/nginx/dist/charts;
    index index.html;

    add_header Access-Control-Allow-Origin $http_origin always;
    add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
    add_header Access-Control-Allow-Headers * always;
    add_header Access-Control-Allow-Credentials true always;

    location / {
        if ($request_method = OPTIONS) {
            return 204;
        }
        try_files $uri $uri/ /index.html;
    }
}

# Icons
server {
    listen 80;
    server_name icons.ffy-fondue.test *.icons.ffy-fondue.test;

    root /usr/share/nginx/dist/icons;
    index index.html;

    add_header Access-Control-Allow-Origin $http_origin always;
    add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
    add_header Access-Control-Allow-Headers * always;
    add_header Access-Control-Allow-Credentials true always;

    location / {
        if ($request_method = OPTIONS) {
            return 204;
        }
        try_files $uri $uri/ /index.html;
    }
}

# Legacy
server {
    listen 80;
    server_name legacy.ffy-fondue.test *.legacy.ffy-fondue.test;

    root /usr/share/nginx/dist/legacy;
    index index.html;

    add_header Access-Control-Allow-Origin $http_origin always;
    add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
    add_header Access-Control-Allow-Headers * always;
    add_header Access-Control-Allow-Credentials true always;

    location / {
        if ($request_method = OPTIONS) {
            return 204;
        }
        try_files $uri $uri/ /index.html;
    }
}

# Tokens
server {
    listen 80;
    server_name tokens.ffy-fondue.test *.tokens.ffy-fondue.test;

    root /usr/share/nginx/dist/tokens;
    index index.html;

    add_header Access-Control-Allow-Origin $http_origin always;
    add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
    add_header Access-Control-Allow-Headers * always;
    add_header Access-Control-Allow-Credentials true always;

    location / {
        if ($request_method = OPTIONS) {
            return 204;
        }
        try_files $uri $uri/ /index.html;
    }
}

# Rich Text Editor
server {
    listen 80;
    server_name rte.ffy-fondue.test *.rte.ffy-fondue.test;

    root /usr/share/nginx/dist/rte;
    index index.html;

    add_header Access-Control-Allow-Origin $http_origin always;
    add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
    add_header Access-Control-Allow-Headers * always;
    add_header Access-Control-Allow-Credentials true always;

    location / {
        if ($request_method = OPTIONS) {
            return 204;
        }
        try_files $uri $uri/ /index.html;
    }
}
