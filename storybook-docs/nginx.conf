# Main storybook-docs site
server {
    listen 80 default_server;
    server_name ${STORYBOOK_DOMAIN} *.${STORYBOOK_DOMAIN};

    root /usr/share/nginx/dist/docs;
    index index.html;

    add_header Access-Control-Allow-Origin $http_origin always;
    add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
    add_header Access-Control-Allow-Headers * always;
    add_header Access-Control-Allow-Credentials true always;

    location / {
        try_files $uri $uri/ =404;
    }
}

# Components
server {
    listen 80;
    server_name components.${STORYBOOK_DOMAIN} *.components.${STORYBOOK_DOMAIN};

    root /usr/share/nginx/dist/components;
    index index.html;

    add_header Access-Control-Allow-Origin $http_origin always;
    add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
    add_header Access-Control-Allow-Headers * always;
    add_header Access-Control-Allow-Credentials true always;

    location / {
        try_files $uri $uri/ =404;
    }
}

# Charts
server {
    listen 80;
    server_name charts.${STORYBOOK_DOMAIN} *.charts.${STORYBOOK_DOMAIN};

    root /usr/share/nginx/dist/charts;
    index index.html;

    add_header Access-Control-Allow-Origin $http_origin always;
    add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
    add_header Access-Control-Allow-Headers * always;
    add_header Access-Control-Allow-Credentials true always;

    location / {
        try_files $uri $uri/ =404;
    }
}

# Icons
server {
    listen 80;
    server_name icons.${STORYBOOK_DOMAIN} *.icons.${STORYBOOK_DOMAIN};

    root /usr/share/nginx/dist/icons;
    index index.html;

    add_header Access-Control-Allow-Origin $http_origin always;
    add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
    add_header Access-Control-Allow-Headers * always;
    add_header Access-Control-Allow-Credentials true always;

    location / {
        try_files $uri $uri/ =404;
    }
}

# Legacy
server {
    listen 80;
    server_name legacy.${STORYBOOK_DOMAIN} *.legacy.${STORYBOOK_DOMAIN};

    root /usr/share/nginx/dist/legacy;
    index index.html;

    add_header Access-Control-Allow-Origin $http_origin always;
    add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
    add_header Access-Control-Allow-Headers * always;
    add_header Access-Control-Allow-Credentials true always;

    location / {
        try_files $uri $uri/ =404;
    }
}

# Tokens
server {
    listen 80;
    server_name tokens.${STORYBOOK_DOMAIN} *.tokens.${STORYBOOK_DOMAIN};

    root /usr/share/nginx/dist/tokens;
    index index.html;

    add_header Access-Control-Allow-Origin $http_origin always;
    add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
    add_header Access-Control-Allow-Headers * always;
    add_header Access-Control-Allow-Credentials true always;

    location / {
        try_files $uri $uri/ =404;
    }
}

# Rich Text Editor
server {
    listen 80;
    server_name rte.${STORYBOOK_DOMAIN} *.rte.${STORYBOOK_DOMAIN};

    root /usr/share/nginx/dist/rte;
    index index.html;

    add_header Access-Control-Allow-Origin $http_origin always;
    add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
    add_header Access-Control-Allow-Headers * always;
    add_header Access-Control-Allow-Credentials true always;

    location / {
        try_files $uri $uri/ =404;
    }
}
