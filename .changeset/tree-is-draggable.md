---
"@frontify/fondue-components": minor
"@frontify/fondue": minor
---

feat(Tree): add `isDraggable` to `Tree.Item` and `Tree.Folder`; a row with `isDraggable={false}` shows no grab handle and cannot be dragged by pointer or keyboard
