---
"@frontify/fondue-components": patch
"@frontify/fondue": patch
---

feat(Tree): rows no longer have to be direct children of `Tree.Root` / `Tree.Folder` — fragments, DOM elements, context providers and wrapper components that receive rows as `children` are seen through
