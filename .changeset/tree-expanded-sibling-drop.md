---
"@frontify/fondue-components": patch
"@frontify/fondue": patch
---

fix(Tree): let a drop land right after an expanded sibling folder that rejects it, and hide the drop line and row highlight while the hovered position is rejected
