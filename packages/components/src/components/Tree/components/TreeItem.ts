/* (c) Copyright Frontify Ltd., all rights reserved. */

import { type TreeItemProps } from '../types';

export const TreeItem = (_props: TreeItemProps): null => null;
TreeItem.displayName = 'Tree.Item';
