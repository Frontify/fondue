/* (c) Copyright Frontify Ltd., all rights reserved. */

import { type TreeIconProps } from '../types';

export const TreeIcon = (_props: TreeIconProps): null => null;
TreeIcon.displayName = 'Tree.Icon';
