name: Fondue Documentation CI

on:
    pull_request:

# Ensures that only one workflow per branch will run at a time.
concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

jobs:
    storybook:
        name: Storybook
        runs-on: ubuntu-latest
        timeout-minutes: 10

        steps:
            - name: Checkout default branch
              uses: actions/checkout@v4

            - name: Use pnpm
              uses: pnpm/action-setup@v3
              with:
                  version: latest
                  run_install: false

            - name: Use Node.js
              uses: actions/setup-node@v4
              with:
                  node-version-file: ".nvmrc"
                  cache: "pnpm"

            - name: Install npm dependencies
              run: pnpm i --frozen-lockfile

            - name: Build Icon package
              run: pnpm --stream --filter {packages/icons} build:generate-react-icons

            - name: Build Charts package
              run: pnpm --stream --filter {packages/charts} build

            - name: Build Storybook packages
              run: pnpm --stream --filter {packages/fondue} --filter {packages/ui} build:storybook

            - name: Build Storybook docs
              run: pnpm build:storybook
