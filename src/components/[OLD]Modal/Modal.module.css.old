/* (c) Copyright Frontify Ltd., all rights reserved. */

.container {
    position: fixed;
    z-index: 1000;
    top: 0;
    left: 0;
    display: flex;
    width: 100%;
    height: 100%;
    align-items: center;
    justify-content: center;
    animation: increaseOpacity 0.3s;
    background-color: var(--f-black-opacity-high);
}

.modal {
    width: 800px;
    background-color: var(--f-white);
}

.withPattern {
    display: grid;
    grid-template-columns: 1fr 2fr;
}

.pattern {
    height: 100%;
    background-position: top;
    background-size: cover;
}

.patternColorViolet {
    background: url("/img/blankslate/modal/pattern-element-violet.svg");
}

.patternColorGreen {
    background: url("/img/blankslate/modal/pattern-element-green.svg");
}

.patternColorYellow {
    background: url("/img/blankslate/modal/pattern-element-yellow.svg");
}

.patternColorRed {
    background: url("/img/blankslate/modal/pattern-element-red.svg");
}

.body {
    display: grid;
    padding: var(--f-spacing-14x) var(--f-spacing-16x);
    grid-template-columns: [column1-start] 1fr [column1-end column2-start] 1fr [column2-end];
    grid-template-rows:
        [title-start] auto [title-end] var(--f-spacing-6x) [content-start] auto [content-end] var(--f-spacing-8x)
        [actions-start] auto [actions-end];
}

.title {
    padding-bottom: var(--f-spacing-6x);
    color: var(--f-black-100);
    font-size: var(--f-font-24-size);
    font-weight: bold;
    grid-column: column1-start / column2-end;
    grid-row: title-start / title-end;
    line-height: var(--f-font-24-line-height);
}

.content {
    grid-column: column1-start / column2-end;
    grid-row: content-start / content-end;
}

.actions {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    grid-column: column2-start / column2-end;
    grid-row: actions-start / actions-end;
}

@keyframes increaseOpacity {
    0% {
        opacity: 0;
    }
    100% {
        opacity: 1;
    }
}
