/* (c) Copyright Frontify Ltd., all rights reserved. */
.sidebar {
    display: flex;
    flex-direction: column;
}

.group {
    display: flex;
    flex-direction: column;
}

.group + .group {
    margin-top: var(--f-spacing-9x);
}

.groupRow {
    display: flex;
    align-items: center;
    padding: var(--f-spacing-2x) var(--f-spacing-3x);
    border-radius: 4px;
    color: var(--f-black-80);
    font-size: var(--f-font-13-size);
    line-height: var(--f-font-13-line-height);
    transition: color 0.2s linear, background-color 0.2s linear;
    user-select: none;
}

.groupRow:focus-visible {
    outline: 2px solid var(--f-violet-60);
}

.groupRow + .groupRow {
    margin-top: var(--f-spacing-2x);
}

.groupName {
    padding-left: var(--f-spacing-3x);
    color: var(--f-black-100);
    font-size: var(--f-font-13-size);
    font-weight: bold;
    line-height: var(--f-font-13-line-height);
    user-select: none;
}

.groupName + .groupRow {
    margin-top: var(--f-spacing-3x);
}

.groupRowSelected {
    background: var(--f-black-05);
    color: var(--f-black-100);
    font-weight: bold;
}
