{
    "name": "root",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "pnpm --stream --filter {packages/components} --filter {docs} --parallel dev",
        "build": "pnpm --stream --parallel build",
        "build:fondue": "pnpm --stream --filter {packages/fondue} build",
        "lint": "pnpm --stream --filter {packages/**} --filter {docs} lint",
        "typecheck": "pnpm --stream --filter {packages/**} --parallel typecheck",
        "ci:version": "pnpm build && changeset version",
        "ci:publish": "pnpm build && changeset publish"
    },
    "devDependencies": {
        "@changesets/changelog-github": "^0.5.0",
        "@changesets/cli": "^2.27.1",
        "@frontify/eslint-config-typescript": "^0.16.2",
        "eslint": "^8.48.0",
        "eslint-plugin-notice": "^0.9.10",
        "glob": "^10.3.4",
        "prettier": "^3.0.3",
        "typescript": "^5.2.2"
    }
}
