{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "pnpm run --filter {packages/fondue} build",
    "ci:version": "pnpm build && changeset version",
    "ci:publish": "pnpm build && changeset publish"
  }
}
